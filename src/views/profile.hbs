<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card text-center">
                <div class="card-body">
                    <h3>Bienvenido {{user.fullname}}</h3>
                    <h4>{{user.username}}</h4>
                    <a href="/registros" class="btn btn-primary m-4">
                        Gestionar Actividades
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>